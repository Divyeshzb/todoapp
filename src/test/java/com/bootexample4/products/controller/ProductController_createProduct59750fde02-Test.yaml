# Test generated by RoostGPT for test dm-cloud-github using AI Model gpt

config:
  target: "http://api.yourwebsite.com"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      # Define a valid JSON payload
      - post:
          url: "/product"
          json:
            name: "Test Product"
            price: 100
            description: "This is a test product"
          # Set the headers for the request
          headers:
            Content-Type: "application/json"
          # Assert that the response status code is 200
          # Assert that the response body contains the expected data
          expect:
            - statusCode: 200
            - hasProperty: "id"

  - name: "API request with validation failure"
    flow:
      # Define an invalid JSON payload with missing required fields
      - post:
          url: "/product"
          json:
            price: 100
          # Set the headers for the request
          headers:
            Content-Type: "application/json"
          # Assert that the response status code is 400
          # Assert that the response body contains the expected error message
          expect:
            - statusCode: 400
            - hasProperty: "error"

  - name: "API request with edge case"
    flow:
      # Define a JSON payload with special characters or large data
      - post:
          url: "/product"
          json:
            name: "Test Product with special characters !@#$%^&*()_+"
            price: 100
            description: "This is a test product with large data" + "a" * 5000
          # Set the headers for the request
          headers:
            Content-Type: "application/json"
          # Assert that the response status code is within an acceptable range
          # Assert that the response body matches the expected pattern or behavior
          expect:
            - statusCode: [200, 201, 202]
            - hasProperty: "id"